# Project Standards

> Generated by observing this codebase. Every rule cites its evidence. Machine-enforced
> formatting/lint rules are referenced by config path, not restated. Delete any section
> the repo has no evidence for; move anything uncertain to "Open questions" at the bottom.
>
> Evidence format for each rule: `(Evidence: <config path> | "<doc quote>" | <pattern> — N/M files)`

## Stack

- **Languages:** <e.g. Go 1.22, TypeScript>  (Evidence: `go.mod`, `package.json`)
- **Frameworks / key libs:** <...>  (Evidence: manifest)
- **Architecture:** <one line — e.g. gRPC services + Postgres; SPA + REST API>

## Commands

| Task | Command | Evidence |
|------|---------|----------|
| Format | `<...>` | `Makefile` / `package.json` |
| Lint | `<...>` | |
| Test | `<...>` | |
| Build | `<...>` | |

## Formatting & lint

Formatting and lint are machine-enforced. **The config files are the source of truth — do not restate their rules here.**

- Enforced by: `<.editorconfig, .prettierrc, .golangci.yml, ...>`  (Evidence: those files exist and run in CI via `<workflow>`)
- Run `<format command>` before committing.

## Language & style idioms

- <e.g. Errors are wrapped with `fmt.Errorf("...: %w", err)`>  (Evidence: pattern — N/M error sites)
- <naming / module layout / idiomatic constructs>  (Evidence: ...)

## Routing / handlers
<!-- Skip this whole section if the repo has no HTTP/RPC surface. -->

- <e.g. All routes are defined as protobuf services; hand-written HTTP handlers are forbidden>  (Evidence: ...)

## Auth / authz
<!-- Skip if not applicable. -->

- <mechanism, and where a new route registers its protection>  (Evidence: `<middleware/guard file>`)

## Multi-tenancy / data scoping
<!-- Skip if single-tenant. -->

- <e.g. Every data-access method scopes by `org_id`>  (Evidence: pattern in `<data layer>` — N/M methods)

## Schema / migrations
<!-- Skip if no database. -->

- <column/enum conventions; migration registration step>  (Evidence: ...)

## ORM / model conventions
<!-- Skip if no ORM. -->

- <permitted struct tags / model patterns>  (Evidence: ...)

## Logging

- <logger API and style — e.g. structured `slog` with key-value pairs; no full request/response objects>  (Evidence: pattern — N/M call sites)

## Testing

- <framework; unit-vs-integration expectations; coverage requirements>  (Evidence: `<test dir>`, CI config)

## Docs / codegen sync
<!-- Skip if nothing is generated. -->

- <what must be regenerated/updated when an API or CLI changes>  (Evidence: `<codegen script / CI drift check>`)

## Commit / PR conventions

- <message format — e.g. Conventional Commits; trailer policy; branch naming>  (Evidence: `git log` — N/M recent commits; `<commitlint config>` if present)

## Security defaults

- <secrets handling, input validation, size caps>  (Evidence: ...)

---

## Open questions

Items with no clear evidence or with competing patterns. These are questions for the
maintainers, NOT rules — resolve them before treating this file as authoritative.

- <e.g. Two logging styles coexist (`slog` vs `log.Printf`); which is canonical?>
- <e.g. No tests found for the `cmd/` package — is that intentional?>
